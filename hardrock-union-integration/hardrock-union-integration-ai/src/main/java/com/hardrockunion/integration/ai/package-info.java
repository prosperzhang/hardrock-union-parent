/**
 * External AI provider integration.
 *
 * <p>This module owns provider clients, provider configuration, request mapping,
 * timeout policy and transport-level errors. Business modules should depend on
 * its stable service contracts instead of calling model providers directly.</p>
 */
package com.hardrockunion.integration.ai;
